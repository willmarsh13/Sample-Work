{\rtf1\ansi\ansicpg1252\cocoartf2512
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\margl1440\margr1440\vieww10800\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 package CheckWord;\
import java.io.*;\
import java.util.*;\
public class CheckString \{\
//check if the first character of the parameter is a letter\
    public static void checkWord(String word) throws Exception \{\
        if( ! Character.isLetter(word.charAt(0))) \{\
        throw new Exception("This is not a word.");\
        \}\
    \}\
    //Prompts user for a string and checks if it's a word\
    public static String getWord() \{\
        System.out.print("Enter a word: ");\
        String input;\
        try (Scanner inputScanner = new Scanner(System.in)) \{\
            input = null;\
            try \{\
                input = inputScanner.nextLine();\
                checkWord(input);\
            \} catch (Exception e) \{\
                System.out.println("Error: " + e.getMessage());\
            \}\
        \}\
    return input;\
    \}\
    //Writes the contents of the array to the file given\
    public static void writeFile(String arrayToWrite[], String fileName) \{\
        BufferedWriter writer = null;\
        try \{\
            //Opens the file \
            writer = new BufferedWriter( new FileWriter(fileName));\
            //Writes the array to the file (line by line)\
            for (String arrayToWrite1 : arrayToWrite) \{\
                writer.write(arrayToWrite1);\
                writer.newLine();\
            \}\
        \} catch (Exception e) \{\
        //Catches exceptions and prints the error message\
        System.out.println("Unable to write to file - " + fileName + " Error: " + e.getMessage());\
        \} finally \{\
            //Closes the file\
            if(writer != null) \{\
                try \{\
                    writer.close();\
                \} catch (Exception e) \{\
                \}\
            \}\
        \}\
    \}\
    public static ArrayList<String> readFile(String fileName) \{\
        //ArrayList to hold the file contents\
        ArrayList<String> fileContents = new ArrayList<>();\
        BufferedReader bufReader = null;\
        try \{\
            bufReader = new BufferedReader( new FileReader(fileName));\
            String line = null;\
            //Read each line from the file and add to the ArrayList\
            while( (line = bufReader.readLine()) != null) \{\
                fileContents.add(line);\
            \}\
        \} catch (Exception e) \{\
            System.out.println("Unable to read the file - " + fileName + " Error: " + e.getMessage());\
        \} finally \{\
            //Close the file stream\
            if(bufReader != null) \{\
                try \{\
                    bufReader.close();\
                \} catch (Exception e) \{\
                \}\
            \}\
        \}\
    return fileContents;\
    \}\
    public static void main(String[] args) \{\
        try \{\
            //call getWord() and print the result on command line.\
            String input = getWord();\
            System.out.println("User entered the input: " + input);\
            //create an array of Strings called testData and populate it with at least three elements.\
            String randomStrings[] = \{"random", "Popular", "Eagle"\};\
            //call writeFile() method passing the array you created in and the String "data.txt".\
            writeFile(randomStrings, "data.txt");\
            //call the readFile() method to read the file you wrote.\
            //Assign the result of readFile() to an ArrayList variable in main called fileContents.\
            ArrayList<String> fileContents = readFile("data.txt");\
            //write a loop to print the contents of the fileContents ArrayList to the command line.\
            for(int i = 0; i < fileContents.size(); i++) \{\
                System.out.println(fileContents.get(i));\
            \}\
        \} catch (Exception e) \{\
            System.out.println("Error: " + e.getMessage());\
        \}\
    \}\
\}\
}