{\rtf1\ansi\ansicpg1252\cocoartf2512
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\margl1440\margr1440\vieww10800\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 package weightedavgdropsmallest;\
import java.util.ArrayList;\
import java.util.Scanner;\
public class WeightedAvgDropSmallest \{\
    public static void main(String[] args) \{ //main method\
        ArrayList<Double> scores = userInput(); //runs 1st method and stores it using array list "scores"\
        int lowest = lowestValue(); //runs 2nd method and stores it using int lowest\
        double average = calcAvg(scores, lowest);\
        double weight = calcWeight();\
        printValues(scores, lowest, average, weight);\
    \}\
    public static ArrayList<Double> userInput()\{ //prompts user for scores\
        Scanner scnr = new Scanner(System.in);\
        ArrayList<Double> scores = new ArrayList<Double>(); //creates an empty array list\
        System.out.print("Please enter 5 values : "); //prompts the user for 5 values\
        for(int i=0; i<5; i++)\{ //for loop fills in the array list\
            scores.add(scnr.nextDouble()); //stores as doubles in the array\
        \}\
        System.out.println(); //formatting\
        return scores; //returns array list double "scores"\
    \}\
    public static int lowestValue()\{ //prompts users for how many scores they want dropped\
        Scanner scnr = new Scanner(System.in); \
        System.out.print("Please enter how many scores you want dropped: "); //prompts user\
        int numScores = scnr.nextInt(); //stores as an int\
        System.out.println(); //formatting\
        return numScores; //returns int "numScores"\
    \}\
    public static double calcWeight()\{ //prompts the user for weight of the scores\
        Scanner scnr = new Scanner(System.in); //establishes object scnr\
        System.out.print("Please enter weight : "); //prompts the user for a weight\
        double weight = scnr.nextDouble(); //stores the weight as a double\
        System.out.println(); //formatting\
        return weight; //returns the double "weight"\
    \}\
    public static double calcAvg(ArrayList<Double> list, int lowest)\{ //calulates average and removes lowest value(s)\
        double minOne = list.get(0); //establishes minOne as an empty double\
        double minTwo = list.get(0); //establishes minTwo as an empty double\
        for(double n:list)\{ //enhanced for loop\
            if(minOne > n)\{ //calculates if minOne is larger\
                minTwo = minOne;\
                minOne = n; //stores as temporary\
            \} else if(minTwo < n)\{ \
                minTwo = n; //stores as temporary\
            \}\
        \}\
        double avg; //establishes avg as a double\
        double total = 0; //establishes "total" as an empty double\
        for(int i=0; i<list.size(); i++)\{ //for loop\
            if(list.get(i) != minOne && list.get(i) != minTwo)\{  //if codition\
                total = total + list.get(i); //calculates total (sum)\
            \}\
        \}\
        avg = total/(list.size()-lowest); //calculates average\
        return avg; //returns "average" as a double\
    \}\
    public static void printValues(ArrayList<Double> list, int lowest, double avg, double weight)\{ //prints all the values requested\
        double wAvg = avg * weight;\
        \
        System.out.print("The weighted average of the numbers is ");\
        System.out.printf("%.2f", wAvg);\
        System.out.print(", when using the data, ");\
        for(int i=0; i<list.size(); i++)\{\
            System.out.print(list.get(i) +", ");\
        \}\
        System.out.println();\
        System.out.println("where "+weight+" is the weight used, and the average is computed after dropping the lowest " +lowest+ "values");\
        \
    \}\
\}}